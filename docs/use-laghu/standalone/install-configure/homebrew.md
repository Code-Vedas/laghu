---
title: Homebrew
parent: Install and configure
grand_parent: Standalone
ancestor: Use Laghu
---
# Homebrew

Install with `brew tap Code-Vedas/tap` and `brew install laghu`. Keep configuration under `/etc/laghu` and runtime state outside the web root.

## Check

Follow the next page only after this step has completed without an installation or configuration error.
