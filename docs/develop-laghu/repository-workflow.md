---
title: Repository workflow
parent: Develop Laghu
---
# Repository workflow

`libs/` owns shared behavior, `modules/` owns native adapters, `servers/laghu/` owns standalone delivery, `workers/` owns isolated transformations, and `packaging/` owns distribution inputs. Keep a change in its owning boundary and update its tests and documentation.
